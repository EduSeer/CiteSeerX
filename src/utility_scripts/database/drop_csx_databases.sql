/*
 * Drops all CSX databases (if exist)
 */
DROP DATABASE IF EXISTS citeseerx;
DROP DATABASE IF EXISTS csx_citegraph;
DROP DATABASE IF EXISTS csx_external_metadata;
DROP DATABASE IF EXISTS myciteseerx;
DROP DATABASE IF EXISTS csxdoi;
